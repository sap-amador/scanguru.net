# OS junk
.DS_Store
**/.DS_Store

# Timestamped patcher backups (every variant ends with _backup_<stamp>)
*_backup_*

# i18n / build tooling — kept local, never served on Pages
*.py
build_i18n.sh
de_cxr.json
en_leaves.json
glossary_*.json
signoff_*.json
untranslated_*.json
/ScanGuru Frontend Backup/

# i18n working files
*.filled.json
en_chunk*.json
